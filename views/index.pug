extends layout

block content
	if locals.authCheck
		h2(class='page-title') SIGN OUT
		a(href='/signout' style='margin-top: 6px;') SIGN OUT

	else 
		h2(class='page-title') SIGN IN
		form(action='/' method='post' id='signin-form')
			div(class='form-group')
				label(for='username') USERNAME
				input(
					type='text'
					class='form-control'
					placeholder='Username'
					name='username'
					value=(messages.username != undefined ? messages.username[0] : '')
					title='Username'
				)
			div(class='form-group')
				label(for='password') PASSWORD
				input(
					type='password'
					class='form-control'
					placeholder='Password'
					name='password'
					value=''
					title='Password'
				)
				if messages.error
					ul(class='error-list')
						li(class='error')= messages.error

			button(type='submit') SIGN IN

			div(id='index-signup')
				p(style='margin-left: 6px; font-size: 0.8em;') Not signed up?
				a(href='/user/create') SIGN UP
				